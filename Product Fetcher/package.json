{
  "name": "product-fetcher",
  "version": "1.0.0",
  "description": "A App which demonstrates the Searching and Filtering of Products",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch" : "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch"
  },
  "author": "Devanjoy Das",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
